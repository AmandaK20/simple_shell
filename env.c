#include "main.h"

/**
 * env_ame - env variblas names with
 * the name passed
 * @name: varibler env
 *
 * Return: (1)
 */
int env_name(char *cmd, char *name)
{
	int i

	for(i = 0; cmd[i] ! '=')
	{
		for(cmd[i] != name[i])
	}
		{

		return (0);

		}
	return(1 + i);

