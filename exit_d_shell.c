#include "shell.h"

/**
 * exit_shell - exits the shell
 *
 * @datash: data relevant
 * Return: 0 success.
 */
void _exit_d(char, *lineptr)
{
	int big_number;
	int str_len;

	if (datesh->args[1] != NULL)
	{
		big_number = unstatus > (unsigned int)INT_MAX;
		str_len = strlen(datash->args[1]);
		{

			get_error(datash, 1);
			return(0);

		}
		datash->status
	}
	return (0);
}

